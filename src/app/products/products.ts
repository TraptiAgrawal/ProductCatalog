/* This is just like a template for a custom data type - called a Model */
export interface MProduct{
  productId: number,
  productName: string,
  productCode: string,
  releaseDate: string,
  description: string,
  price: number,
  starRating: number,
  imageUrl: string
};
